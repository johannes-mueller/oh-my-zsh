export VISUAL=vi
export EDITOR=vi
export LESSOPEN="| source-highlight --failsafe  -f esc -v -i %s"
export LESS="-R -F -M -X $LESS"

if [ -e "$HOME/.zshenv.local" ] ; then
	source "$HOME/.zshenv.local"
fi
